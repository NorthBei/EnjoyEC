include ../filter/filter.jade

mixin banner_section(linkUrl,bannerUrl,navArray,isFilter)
    section#main_section
        a(href=linkUrl)#main_banner.page_link
            img(src=bannerUrl)

    #sub_section
        span#page_info
            .page_nav
                //- a(href="#") 首頁
                //- span.page_nav_separate >
                //- a(href="#") 限時優惠
                each item ,index in navArray
                    a(href="#")= item
                    if index != (navArray.length - 1)
                        span.page_nav_separate >

            .commodity_count 共10項商品
        if isFilter
            +filter()
        //- form
        //-     label.commodity_filter
        //-         select
        //-             option(value='價格低到高') 價格低到高
        //-             option(value='價格高到低') 價格高到低
        //-             option(value='價格不要出現') 價格不要出現
        //-             option(value='我是土豪') 我是土豪